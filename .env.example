# ─── Academia Bolaños — Environment Variables ───────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.

# Public: visible in frontend build (Astro PUBLIC_ prefix)
PUBLIC_SITE_URL=https://nelsystems77.github.io/academia-bolanos
PUBLIC_WA_PHONE=50689215848

# Private: server-side only (if future endpoints are added)
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
